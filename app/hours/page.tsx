import { PhasePending } from "@/components/ui/phase-pending";

export default function HoursPage() {
  return (
    <PhasePending
      title="Hours & Classes"
      phase="Phase 3"
      description="Raw class hours by trainer, cross-checked the way the source workbook was — category by category, with gaps surfaced rather than smoothed over."
    />
  );
}
